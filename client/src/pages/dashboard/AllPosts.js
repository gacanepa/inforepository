import React from 'react';
import { PostContainer, SearchContainer } from '../../components';

const AllPosts = () => (
  <div>
    <SearchContainer />
    <PostContainer />
  </div>
);

export default AllPosts;
